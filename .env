# .env  —  NEVER commit this file (add to .gitignore if not already)
XAI_API_KEY=sk-your-real-key-here-if-you-want-grok-bridge
# Leave empty or delete line above to keep Grok disabled (default = safe & sovereign)

# Optional future bridges
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
